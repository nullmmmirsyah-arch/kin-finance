# P0 Polish Batch — Implementation Plan

> Date: 2026-08-26
> Spec: `docs/superpowers/specs/2026-08-26-p0-polish-design.md`

## Steps

1. **Create `hooks/useDiscardGuard.ts`**
   - `useDiscardGuard({ isDirty })` → `{ handleBack, markIntentional }`.
   - Internal `intentionalLeave` ref; `handleBack` Alerts when dirty;
     `beforeRemove` listener allows clean/intentional passes, otherwise
     prevents + Alerts. Alert copy: "Discard unsaved changes?" /
     "You have unsaved changes that will be lost." / "Keep editing" /
     "Discard" (destructive).

2. **Migrate `app/transaction-form.tsx` onto the hook**
   - Delete local `intentionalBack` ref, `handleBack` callback, and
     `beforeRemove` effect; use `useDiscardGuard({ isDirty: hasInteracted })`.
   - Replace both `intentionalBack.current = true` call sites (submit success,
     delete success) with `markIntentional()`. Drop now-unused
     `useNavigation` import.

3. **Add guards to `app/account-form.tsx`, `app/category-form.tsx`, `app/budget-form.tsx`**
   - Compute `isDirty` per spec §2; wire `handleBack` to header back Pressable;
     call `markIntentional()` before `router.back()` after successful submit.

4. **Un-gate Home budget CTA (`app/(tabs)/home.tsx`)**
   - EmptyState action always rendered ("Create Budget" → `/budget-form`).

5. **AccountCard hidden badge**
   - Optional `hidden?: boolean` prop → eye-off + "Hidden" pill under type
     label (CategoryCard pill styling). Pass `item.hidden` from
     `app/(tabs)/accounts.tsx` (both branches).

6. **transaction-form loading skeleton**
   - Replace plain-text loader block with real back header + Skeleton title/
     subtitle + three section-height blocks + button bar (`Radius.md`).
     Import `Radius`.

7. **OtaUpdater English copy** — swap the snackbar string.

8. **PRD.md updates**
   - Header date; §1 Constraints currency bullet; Appendix A haptics line;
     §3.8 budget CTA note; §4.4 shared-guard sentence; §5.2 components row +
     responsibilities (hook); §5.7 OTA copy quote; two §8 Change Log rows
     dated 2026-08-26.

9. **Verify**: `npx tsc --noEmit`; `npm run lint`; `npm test` (must stay green).

## Verification notes

UI-only change set — no new unit tests required by repo policy (AGENTS.md:
run tests when pure utils or Convex functions change; neither is touched).
