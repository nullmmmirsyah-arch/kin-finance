import * as React from "react";
export const SvgXml = (props: any) => React.createElement("svg", props);
export const Svg = (props: any) => React.createElement("svg", props);
export const Circle = (props: any) => React.createElement("circle", props);
export default { SvgXml, Svg, Circle };
