// Re-export shim — deep Icon module owns the allowlist (single source)
export { ALL_CATEGORY_ICONS, DEFAULT_CATEGORY_ICON, isValidCategoryIcon } from "@/modules/icon-registry/internal";
export type { CategoryIconName } from "@/modules/icon-registry/internal";
